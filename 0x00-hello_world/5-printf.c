#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char p_gramma[70] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", p_gramma);

	return (0);
}
